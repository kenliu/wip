pub async fn run(search_term: Option<String>) -> Result<(), Box<dyn std::error::Error>> {
    // TODO: Implement user mode
    // 1. Load config
    // 2. Load/refresh index (if stale)
    // 3. Filter in-progress sessions
    // 4. Display TUI with session list
    // 5. Handle user selection
    // 6. Launch CLI command for selected session

    println!("User mode not yet implemented");
    Ok(())
}
